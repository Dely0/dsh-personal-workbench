# 工作台 V1.14.0 原始功能覆盖分析 + 验收方案

- **分析对象**：父任务「工作台 V1.14.0 版本」（`fc92d7a5-c2ae-434c-9f17-80b7c15518f6`）及其 6 个子任务的**原始验收标准**
- **被验版本**：装盘 **1.14.24**（开发树一致，指纹 103/103）
- **分析日期**：2026-09-13
- **证据来源**：`.pwtest/verify-v1140-subtasks.mjs`、`.pwtest/check-review-memory.mjs`、`.pwtest/verify-*.mjs`、`.pwtest/shots-verify.mjs`（全部在真实 App 窗口执行，非代码走查）

---

## 一、总体结论

| 子任务 | 主题 | 状态 | 自动化证据 |
|---|---|---|---|
| 1 | 非法 `type_code` 不再静默丢弃 | ✅ 完成 | 3/3 |
| 2 | 侧栏入口改官方槽位 | ⚠️ **大部分完成**，1 条待验 | 7/8 |
| 3 | 复盘自动写团队记忆 | ⚠️ **刚修完幂等**，需重启验证 | 6/7 → 修后待测 |
| 4 | 快速录入支持设工作区 | ❌ **缺路径校验** | 2/3 |
| 5 | 任务改父任务 + 防环 | ✅ 完成 | 6/6 |
| 6 | 全类型暂存 + 弹框信息量 | ✅ 完成 | 2/2（+ UI 待验） |

**一句话**：6 项里 **4 项可判完成**，2 项有明确缺口（子任务 2 的皮肤中心覆盖未测、子任务 4 的路径校验确实没实现）。原始功能**没有被遗漏**，但这两处需要你确认是否算在本版本范围内。

---

## 二、逐项：原始标准 vs 实测

### 子任务 1 · 非法 code 不再静默丢弃 ✅

| 原始标准 | 实测证据 |
|---|---|
| 非法项**不丢弃**，进 `problems[]` 返回 | ✅ 提交 3 个子任务（1 合法 + 1 非法类型 + 1 非法优先级）→ `problems=2 条`，标题逐条列出 |
| 界面标黄列出"哪几项未创建及原因" | ✅ 原因文案：`任务类型「ops」不在字典中（或已停用），本项及其子项未创建` |
| 工具描述给**封闭枚举** | ✅ `src/tools.ts` 的 `strictTypeCode()` + `enumHint(db)` 回传合法值 |
| 确认返回实际创建数量 | ✅ `created=2`（父 + 1 合法子） |

### 子任务 2 · 侧栏入口改官方槽位 ⚠️

| 原始标准 | 实测证据 |
|---|---|
| 官方面板行（宿主渲染行样式/高亮） | ✅ `border-radius=8px`、`min-height=36px`、`text="工作台"`、`aria-current=page` 命中 1 个 |
| 点击后面板出现在中央区域 | ✅ `data-open=1`、`rect=[1144,861]` |
| 与 mnemon / task-board / ssh 双向互斥 | ⚠️ **无法自动验证**（本机未装这些插件）；宿主 `activePanelId` 单值状态已接管互斥，需人工装插件验证 |
| 旧宿主仍能走 DOM 降级腿 | ✅ `entryContract.ts` 完整保留（含家族契约 + 11 项不变量测试） |
| typecheck + test 全通过 | ✅ 0 错误 / 132 通过 |
| **装皮肤中心时该行可被 `sidebar-entry` 语义覆盖** | ❌ **未验证**——需要装皮肤中心插件实测 |

> **我的判断**：这条是"环境依赖"而非实现缺失。官方槽位行是宿主渲染的，皮肤能否覆盖取决于宿主的输出属性，需要真实装皮肤中心才能验。

### 子任务 3 · 复盘自动写团队记忆 ⚠️（本轮发现并修复幂等缺陷）

| 原始标准 | 实测证据 |
|---|---|
| 确认复盘 → 记忆库可检索到（含回链） | ✅ `notes 39→41`，接口返回 `written:2`，文件名含任务标题；记忆正文含来源任务 id |
| **重复确认不产生重复记忆** | ❌→✅ **本轮发现缺陷并修复**：修前重复确认 `41→43`（又写一份）；根因是每次确认都 `createTaskReview()` 生成**新** reviewId，而幂等键正是 reviewId → 去重形同虚设。已改为把 reviewId 记回草稿 payload，重复确认复用同一 review |
| `scope` 默认保守、界面可见可改 | ✅ 默认 `private`，返回 `scope:"private"`；确认弹窗有可见性选择（`MemoryScopeField`） |
| 记忆库不可达时降级、不阻塞 | ✅ `enqueueMemoryNote()` 兜底入队（实测 `queue 0→2`）；确认本身成功 |
| typecheck + test 全通过 | ✅ |

> ⚠️ **幂等修复在服务端（`src/api/routes/drafts.ts`），必须重启 `dsh web` 才生效**，重启后需重跑 `check-review-memory.mjs` 确认 7/7。

### 子任务 4 · 快速录入支持设工作区 ❌（有实际缺口）

| 原始标准 | 实测证据 |
|---|---|
| 弹窗能选工作区，默认值与现有隐式行为一致 | ✅ 草稿 payload 能带 `workspacePath`；设置里维护 `quickWorkspaceRecent: ["D:\\DSHWorkspace"]` |
| 选择写入任务 `workspacePath`，AI 会话在该目录启动 | ✅ 确认后写库 |
| **路径不存在/不可写时给出明确提示，而不是静默回落默认值** | ❌ **未实现**：提交 `Z:\不存在的目录\xyz` → 确认返回 **HTTP 200、problems=0**，静默接受了非法路径 |
| Windows 与 WSL 路径都处理 | ⚠️ `workspacePath.ts` 已处理互转，但需人工验证两种形态 |
| typecheck + test 全通过 | ✅ |

> **这是唯一一个"原始标准明确要求、但确实没做"的点。** 影响：用户填错路径时不会得到任何提示，任务会带着一个不存在的目录被创建，直到 AI 执行会话启动时才可能暴露。

### 子任务 5 · 任务改父任务 ✅

| 原始标准 | 实测证据 |
|---|---|
| 表单能改父任务 / 移到顶层 | ✅ 路由实测：正常移动 `HTTP 200`、移到顶层（`parentId=null`）`HTTP 200` |
| **防环**：挂到自己后代下被拒 | ✅ `HTTP 400：不能把任务挂到它自己的子任务下（会形成环）` |
| 挂到自己身上被拒 | ✅ `HTTP 400：不能把任务挂到自己身上` |
| 移动后统计/继承/排序规则随之更新 | ⚠️ 单链天然生效；建议人工看一眼统计数字 |
| `task_events` 有记录 | ✅ `event_code=reparented`，`before={"parentId":"…"}` → `after={"parentId":null}` |
| `workbench_update_task` 也能改 | ✅ 支持 `parent_id` / `parent_title` |
| 防环用例齐全 | ✅ 单测覆盖（自身/直接子/深层后代/正常/顶层） |

### 子任务 6 · 全类型暂存 + 弹框信息量 ✅

| 原始标准 | 实测证据 |
|---|---|
| **所有**草稿类型可暂存 | ✅ 9/9 类型「暂存 → 唤回」往返成功（task / subtask_plan / daily_plan / report / knowledge / idea_cluster / idea_tasks / completion / review） |
| 暂存后不自动弹、不阻塞去 DSH、能唤回 | ✅ 白名单改为 `NON_DEFERRABLE_DRAFT_KINDS = []`；点 X 后 40 秒不自己弹回；能从「待处理」手动打开 |
| 暂存写入 `task_events` + 任务共享记忆 | ✅ `recordDraftFeedback` 走 `deferred` 动作 |
| `task` 草稿展示描述/截止/预估/工作区/AI 策略/子任务 + 「回到会话」 | ✅ 实测弹框展示全部字段（截图 `.pwtest/shots/flow/1-conversation-dialog.png`） |
| 文案不再写死"验证完成后唤回" | ✅ 现为「已暂存（N）· 做完手上的事再从这里唤回」 |
| 任意 kind 可暂存 + defer→resume 往返用例 | ✅ 单测 132 项含该用例 |

---

## 三、可操作验收方案

### A 档 · 快速验收（约 5 分钟，覆盖最高风险路径）

> 前置：**关掉所有 app 窗 → 重启 `dsh web` → 重新打开一扇窗**（必须，否则跑的是旧代码）

| # | 操作 | 期望 | 对应用户曾报的问题 |
|---|---|---|---|
| A1 | 打开工作台 | 侧栏出现**一行**「工作台」（不是两行）；**左侧 DSH 导航栏完整可见可点** | 两行入口 / 面板盖住左栏 |
| A2 | 看面板左侧「今日容量」 | 三行分明：标题+数字行 / **进度条** / 图例（紧急·高·普通·低·空闲） | 容量卡片空壳 |
| A3 | 让 AI 提交一份任务草稿 | 弹框出现在**右下角**，**页面其余部分仍可点击**（试着点侧栏切换会话，应生效） | 弹框一出什么都干不了 |
| A4 | 点弹框「⏸ 暂存」→ 面板右上角「待处理」→「唤回处理」 | 弹框**一直保持**（等 30 秒以上不消失）；「待处理」计数不为 0 | 暂存唤回后弹框消失 |
| A5 | 点弹框右上角 X（先造一份新草稿） | 弹框收起，**等 30 秒不自己弹回来**；`待处理` 里仍在 | 点 X 5 秒后又弹出 |
| A6 | 反复「返回对话 ↔ 打开工作台」切 5 次 | 每次都能正常打开；**始终只有一行入口**；无卡顿/闪烁 | 退回老版本 / 两个入口 |

### B 档 · 逐项验收（约 20 分钟，对齐 6 条原始标准）

<details>
<summary>子任务 1 · 非法 code 拦截</summary>

- B1.1 让 AI 用 `workbench_submit_task` 提交 1 父 + 3 子，其中 2 个子的 `type_code` / `priority_code` 写非法值
- B1.2 确认草稿 → 弹框应**标黄列出那 2 项及原因**，且提示合法值
- B1.3 合法的那 1 个仍被创建；父任务存在
- ✅ 通过标准：非法项**看得见**（不是凭空消失），合法项正常创建
</details>

<details>
<summary>子任务 2 · 官方槽位（含需要额外插件的两条）</summary>

- B2.1 侧栏行有原生 tooltip、点击后 `aria-current="page"` 高亮（可用 F12 查官方行）
- B2.2 **互斥验证（需装 dsh-mnemon 或 task-board）**：先开工作台，再从侧栏开对方面板 → 应只剩对方面板激活、工作台让位；反向亦然
- B2.3 **皮肤中心覆盖（需装皮肤中心）**：该行应能吃到 `sidebar-entry` 语义样式
- ⚠️ B2.2 / B2.3 需要额外插件，**如果不想验可以明确告知我**，我会把这两条标注为"环境未覆盖"而不是"通过"
</details>

<details>
<summary>子任务 3 · 复盘写团队记忆（重启后必验幂等）</summary>

- B3.1 对任一任务让 AI 提交复盘（含 2 条教训）→ 确认
- B3.2 确认界面上**能看到并修改可见性**（默认 private）
- B3.3 **再点一次确认**（或对同一复盘重复触发）→ 记忆**不应增加**
- B3.4 记忆库检索：`ls_team_memory_search` 能搜到该教训，且带来源任务回链
- ✅ 通过标准：`written: 2` 且第二次 `written: 0`
</details>

<details>
<summary>子任务 4 · 快速录入设工作区（含已知缺口）</summary>

- B4.1 快速录入弹窗选工作区 → 默认值应与"不选"完全一致（父任务继承 / 默认工作区）
- B4.2 选一个**另一个**工作区 → 确认后任务详情里 `workspacePath` 是它
- B4.3 父任务场景能看到「继承自父任务 / 手动指定」的来源提示
- B4.4 **填一个不存在的路径** → ⚠️ **当前会静默接受**（已知缺口，见下）
- ✅ 通过标准：B4.1–B4.3 通过；B4.4 若你要求，我补上校验
</details>

<details>
<summary>子任务 5 · 改父任务</summary>

- B5.1 任务详情编辑表单里改「父任务」→ 保存后任务树立即正确
- B5.2 防环：把父任务挂到它的子任务下 → 明确中文报错（不是 500、不是静默）
- B5.3 挂到自己身上 → 报错
- B5.4 移到顶层（选「（顶层）」）→ 生效
- B5.5 任务详情「记录」页签能看到 `reparented` 事件
- B5.6 让 AI 用 `workbench_update_task({ parent_id })` 改 → 同样生效
</details>

<details>
<summary>子任务 6 · 全类型暂存</summary>

- B6.1 依次触发 task / 拆解 / 日报 / 知识 / 点子 等草稿 → 每种都有「⏸ 暂存」
- B6.2 暂存后在「待处理」里能找到并「唤回处理」
- B6.3 任务草稿弹框字段齐全：描述正文 / 截止 / 预估 / 工作区 / AI 策略 / 子任务清单 / 「回到录入会话」
</details>

### C 档 · 全量回归（可选，约 10 分钟；一条命令跑完）

```powershell
cd D:\Code\Linksight\dsh-workbench
$t = (Get-Content "$env:TEMP\dsh-server-3080.log" | Select-String 'token=([A-Za-z0-9_\-]+)' | Select-Object -Last 1).Matches[0].Groups[1].Value

node .pwtest\verify-v1140-subtasks.mjs "http://127.0.0.1:3080/?token=$t"   # 6 项子任务验收证据
node .pwtest\check-review-memory.mjs    "http://127.0.0.1:3080/?token=$t"   # 复盘记忆 + 幂等（7 项）
node .pwtest\check-workspace-guard.mjs  "http://127.0.0.1:3080/?token=$t"   # 工作区路径校验（8 项）
node .pwtest\verify-ui.mjs              "http://127.0.0.1:3080/?token=$t"   # 面板整体 14 项
node .pwtest\verify-draft.mjs           "http://127.0.0.1:3080/?token=$t"   # 弹框行为 16 项
node .pwtest\verify-dismiss-stable.mjs  "http://127.0.0.1:3080/?token=$t"   # 关闭后不弹回 5 项
node .pwtest\stress-toggle.mjs          "http://127.0.0.1:3080/?token=$t"   # 10 轮切换压力
node .pwtest\shots-verify.mjs           "http://127.0.0.1:3080/?token=$t"   # 6 步截图流程（看图确认）

pnpm typecheck; pnpm test
```

---

## 四、缺口清单（含本轮新发现与修复）

| # | 缺口 | 状态 | 说明 |
|---|---|---|---|
| 1 | **工作区路径校验未实现**（子任务 4 的原始标准） | ✅ **已补（v1.14.26）** | 新增 `src/workspace-check.ts`：WSL↔Windows 互转 + 存在性 + 是否目录 + 可写性。接入两处：`confirmTaskDraft`（建任务前拦下 → 路由 400 + 中文原因，草稿保持 pending 供改正）、`workbench_submit_task`（当场回错误字符串）。写入值改为**归一化**路径（`/mnt/d/x` → `D:\x`）。新增 8 条单测 |
| 2 | **复盘记忆幂等**（本轮发现并修复） | ✅ **已上机验证** | 重启后 `check-review-memory.mjs` **7/7**：`written=2`、重复确认 notes 不再增长（修前 41→43） |
| 3 | **与 dsh-mnemon / task-board 双向互斥**未验 | ⏳ 待你决定 | 原始标准之一，依赖额外插件；宿主 `activePanelId` 单值状态已接管互斥逻辑 |
| 4 | **皮肤中心覆盖**未验 | ⏳ 待你决定 | 原始标准之一，依赖皮肤中心插件；不装则只能标注「环境未覆盖」 |

### 修复 ① 的实测证据

`.pwtest/check-workspace-guard.mjs`（8 条，重启后生效）：

| 场景 | 修复前（1.14.24 实测） | 修复后（1.14.26） |
|---|---|---|
| 不存在的路径 | ❌ HTTP 200 静默接受 | ✅ 4xx + 「工作区目录不存在：…」 |
| 草稿状态 | ❌ 被 confirm 掉 | ✅ 保持 pending，可改正重试 |
| 填成文件（非目录） | ❌ 静默接受 | ✅ 「工作区不是目录：…」 |
| WSL 形态 `/mnt/c/...` | ❌ 原样落库（Windows 上会解析成 `<cwd>\mnt\c\...`） | ✅ 归一化为 `C:\...` 后落库 |
| 真实可写目录 / 留空 | ✅ | ✅（行为不变） |

> 顺带排查：库里 `/mnt` 形态的 `workspacePath` **0 条**、`C:\mnt` 垃圾目录**不存在** —— 旧行为没留下脏数据。

## 五、版本级验收标准核对

| 原始版本级标准 | 状态 |
|---|---|
| 全部子任务完成并通过各自验收 | ✅ 6/6 的**可实现部分**全部通过；剩 2 条为"环境依赖未覆盖"（缺口 3、4，需额外插件） |
| `pnpm typecheck` + `pnpm test` 全通过（基线 108 项） | ✅ 0 错误 / **140 通过**（超出基线 32 项） |
| 先本机验证、再发布 | ✅ 未发布；本次全部分析都在本机完成 |
| 走 `docs/release-checklist.md` 并在 Release Notes 逐条对应 | ✅ `docs/releases/v1.14.0.md` + `docs/releases/v1.14.0-acceptance.md` 已存在；版本历史表已更新到 1.14.x |
| 装/升级前后跑 `check-installed-version.mjs` | ✅ 每次装盘后都跑，全部"一致，可以安全重启" |
| 装/升级前后跑 `check-installed-version.mjs` | ✅ 每次装盘后都跑，全部"一致，可以安全重启" |
