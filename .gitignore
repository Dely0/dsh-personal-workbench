node_modules/
lib/
*.tgz
.pwtest/
test-results/

# ── 本机运行产物：不要进仓库 ────────────────────────────────────────────────
# 版本存档：`node scripts/make-archive.mjs <版本>` 生成的目录。
# 里面是**装盘包 + 整份装盘副本**（含 lib、screenshot 等），单个几 MB；
# 而 tgz 本来就在仓库根目录留档（且已被上面的 `*.tgz` 忽略）。
# 存档的意义是"本机可追溯、可回退"，不是"进版本库"——提交二进制只会让仓库膨胀。
_archive-*/

# WSL 装盘前的备份目录：每个含一份完整装盘副本，**单份可达 60+ MB**
#（实测 4 个目录合计 262 MB）。绝不入库。
_wsl-backup-*/

# 各类临时备份（改配置 / 改依赖 / 改源码前的快照）
*.bak-*
src/**/*.bak-*
